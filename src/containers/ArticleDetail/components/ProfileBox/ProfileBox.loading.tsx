import styled from "@emotion/styled"
import React from "react"

type Props = {}

const ProfileBoxLoading: React.FC<Props> = ({}) => {
  return <StyledWrapper>로딩중인데요.</StyledWrapper>
}

export default ProfileBoxLoading

const StyledWrapper = styled.div``
