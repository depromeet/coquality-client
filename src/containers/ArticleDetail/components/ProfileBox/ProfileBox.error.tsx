import styled from "@emotion/styled"
import React from "react"

type Props = {}

const ProfileBoxError: React.FC<Props> = ({}) => {
  return <StyledWrapper>에러가 발생했습니다.</StyledWrapper>
}

export default ProfileBoxError

const StyledWrapper = styled.div``
