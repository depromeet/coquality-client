import styled from "@emotion/styled"
import React from "react"

type Props = {}

const CommentListEmpty: React.FC<Props> = ({}) => {
  return <StyledWrapper></StyledWrapper>
}

export default CommentListEmpty

const StyledWrapper = styled.div``
